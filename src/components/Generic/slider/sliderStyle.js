import { StyleSheet } from "react-native";

export const sliderStyle = StyleSheet.create({
  sliderContainer: {
    marginBottom: 20,
  },
  title: {
    fontSize: 20,
    fontWeight: "500",
    marginBottom: 5,
    textAlign: "center",
    marginBottom: 0,
  },
  subTitle: {
    fontSize: 15,
    marginBottom: 20,
    textAlign: "center",
  },
});
